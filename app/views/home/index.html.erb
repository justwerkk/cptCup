<% content_for :head do %>
<style>
  .experimental {
    position: relative;
    float:right;
    padding-right: 1em;
  }
  .experimental sup {
    color: red;
    font-weight: bold;
    -moz-transform: rotate(-27deg);
    -webkit-transform: rotate(-27deg);
    display: block;
    left: -2em;
    position: relative;
    top: -1em;
    font-size: 0.538em;
  }
</style>
<% end %>

<div class="experimental">
  <sup>New</sup>
  <a href="<%= comparisons_path %>">W-L Records</a>
</div>

<h1>Canuto Tacderan Cup</h1>

<%= image_tag "canuto.jpeg", :style => "height:400px;margin-left:300px;position:absolute;" %>
<%= render 'games/form' %>

<h4>Leaderboard</h4>
<ul>
  <% @players_hash.sort_by {|k,v| v}.reverse.each do |k,v| %>
    <li><%= "#{k}: #{v}" %>
  <% end %>
</ul>
<p>Total Number of Games Recorded: <%= @num_games %></p>

<a href="<%= games_path %>">List all games</a>

<%= render 'players/form' %>
